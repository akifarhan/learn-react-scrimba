- syntax extension of JS
- embedding JS fn in HTML

const name = 'Akif';  //JS
const element = <h1> Hello , {name} </h1>; // JS in HTML

comment = {/* ... */}

ReactDOM.render(component, targetNode);

can self closed;
<br /> == <br> </br>